# Test 06: Invalid observation type
# Expected error: ValidationError or registry lookup failure

analysis:
  start_time: "2024-02-01"
  end_time: "2024-02-03"
  output_dir: ./output

model:
  cesm:
    mod_type: cesm_fv
    files: /some/path/*.nc

obs:
  bad_obs:
    obs_type: fake_observation_type_abc
    filename: /some/path/obs.nc
