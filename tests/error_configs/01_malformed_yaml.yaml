# Test 01: Malformed YAML - syntax error
# Expected error: YAML parsing error

analysis:
  start_time: "2024-02-01"
  end_time: "2024-02-03"
  output_dir: ./output

sources:
  test_dataset:
    type: cesm_fv
    files: /nonexistent/path/*.nc
    # Missing closing quote on next line
    variables:
      O3:
        unit_scale: 1.0e9
        geometry_name: "ozone
