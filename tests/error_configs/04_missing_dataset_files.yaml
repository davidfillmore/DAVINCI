# Test 04: Dataset files don't exist
# Expected error: DataNotFoundError - files not found

analysis:
  start_time: "2024-02-01"
  end_time: "2024-02-03"
  output_dir: ./output

sources:
  cesm_run:
    type: cesm_fv
    files: /this/path/definitely/does/not/exist/dataset_*.nc
    variables:
      O3:
        unit_scale: 1.0e9


  surface:
    type: pt_sfc
    filename: /also/nonexistent/geometry.nc
