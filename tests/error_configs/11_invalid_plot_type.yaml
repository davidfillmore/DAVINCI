# Test 11: Invalid plot type
# Expected error: Validation or registry error

analysis:
  start_time: "2024-02-01"
  end_time: "2024-02-03"
  output_dir: ./output

sources:
  cesm:
    type: cesm_fv
    files: /some/path/*.nc


  surface:
    type: pt_sfc
    filename: /some/path/geometry.nc

pairs:
  cesm_surface:
    dataset: cesm
    geometry: surface
    variable:
      dataset_var: O3
      geometry_var: ozone

plots:
  bad_plot:
    type: fake_plot_type_xyz
    pairs: [cesm_surface]
