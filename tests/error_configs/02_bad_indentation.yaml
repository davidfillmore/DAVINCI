# Test 02: Bad YAML indentation
# Expected error: YAML parsing error

analysis:
  start_time: "2024-02-01"
  end_time: "2024-02-03"
output_dir: ./output  # Should be indented under analysis

sources:
  test_dataset:
  type: cesm_fv  # Should be indented under test_dataset
    files: /nonexistent/path/*.nc
